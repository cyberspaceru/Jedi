{
  "static_fields": [
    "Название страницы",
	"Имя класса Page Object"
  ],

  "table_columns": [
    "Название элемента:target:",
    "Тип:select:['Button', 'Select', 'InputText', 'Link']",
	"Имя переменной"
  ]
}

$code
{
$declaration {name: 'BDD Java', type: 'java'}
$pattern
{
@Title("$eval(static['Название страницы'])")
public class $eval(static['Имя класса Page Object']) {
	
	#startsection
	@FindBy(xpath = "$eval(row['xpath'].replace(/"/g, '\\"'))")
	@ElementTitle("$eval(row['Название элемента'])")
	public WebElement $eval(row['Тип'].toLowerCase().replace(/[aeiou]/gi, '') + row['Имя переменной']);
	
	#endsection

	
    @Override
    public void заполняет_форму() throws Throwable {
	}
	
}
}
}